<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Main Route</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h1>Quora</h1>
    <% for(post of posts){ %>
    <!-- loops, conditional formats, functions <% %> -->
    <!-- To access the data which comes outside or from another file. <%= %> -->
    <div class="post">
      <h3 class="user">@<%= post.username %></h3>
      <p><%= post.content %></p>
      <a href="http://localhost:8080/posts/<%= post.id %>">Show in detail</a>
      <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
      <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
        <button>Delete</button>
      </form>

    </div>
    <% } %>
    <br />
    <a href="http://localhost:8080/posts/new" class="create">Create new Post</a>
  </body>
</html>